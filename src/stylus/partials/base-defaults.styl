html
    font-size 62.5%

body
    font-size 1.6rem
    line-height 2rem 
    font-family 'open-sans', sans-serif
    padding border-width
    +above(beta)
        padding (border-width * 2) (border-width * 4)
        min-height 100%
        min-height 100vh

img
    max-width 100%
    height auto